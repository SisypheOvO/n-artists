# TODO List

- [ ] Allow storing the list to Google Drive to use the list across devices and avoid losing it (for example, when your browser data is cleared).
- [ ] Allow searching for artists in the `/favorites/` page.
- [ ] Allow sorting the artists in the `/favorites/` page by name or by the number of favorites.
- [ ] Allow subscribing to artists in the `/favorites/` page to get notified when they upload new works.
